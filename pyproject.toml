[build-system]
requires = [
    # latest at time of writing
    "setuptools >= 44.1.1",
    "wheel >= 0.37.0",
]
build-backend = 'setuptools.build_meta'

[tool.black]
preview = true
target-version = ['py38']

[tool.isort]
profile = 'black'
